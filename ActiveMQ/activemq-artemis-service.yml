apiVersion: v1
kind: Service
metadata:
  name: activemq
spec:
  ports:
  - port: 1883
    targetPort: 1883
    name: mqtt
  - port: 8161
    targetPort: 8161
    name: web
  selector:
    app: activemq
  type: LoadBalancer
